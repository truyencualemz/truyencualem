/* ── CONFIG.JS ────────────────────────────────────────────
   Copy file này thành config.js rồi điền thông tin thật.
   File config.js đã có trong .gitignore — không bị push lên Git.

   Lấy thông tin tại:
   Supabase Dashboard → Project Settings → API
──────────────────────────────────────────────────────────── */
window.SUPABASE_URL  = 'https://YOUR_PROJECT_ID.supabase.co';
window.SUPABASE_ANON = 'YOUR_ANON_PUBLIC_KEY';
